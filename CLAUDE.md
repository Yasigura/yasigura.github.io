# Личный сайт — контекст проекта

Одностраничный сайт о владельце. Сейчас готов только
первый экран, остальное будем делать постепенно.

Разложено по папкам, без сборки и зависимостей:
- `index.html` — разметка, лежит в корне.
- `css/styles.css` — все стили.
- `js/script.js` — зерно и переключатель звука.
- `assets/angel.jpg` — фото статуи.
- `assets/id_114.mp3` — фоновый звук.

Открывается двойным кликом по index.html, ничего собирать
не нужно.

## Как со мной разговаривать

- По-русски, простым языком, будто я слышу тему впервые.
- Термины пояснять в скобках.
- Ответы средней длины: не стена текста, но и не сухие
  тезисы — нужно нормальное объяснение.
- Тон тёплый, с лёгкой ноткой милоты. Чуть-чуть.
- Если я не прав — говори прямо, не смягчай.

## Внешний вид: что уже решено

Настроение — очень мрачное. Чёрный почти чистый, никакого
сияния, никаких цветных градиентов. Ориентир: тёмная
мраморная скульптура и брутальная типографика, чёрно-белая,
крупная, заглавными.

- `--ink: #070707` — фон.
- `--paper: #E7E4DD` — текст, сероватый, не белый.
- `--ash`, `--ash-dark` — подписи.
- Заголовок: очень узкий тяжёлый гротеск, заглавные,
  межстрочное расстояние плотное (`line-height: .84`).
- Мелкие подписи: моноширинный, вразрядку, в квадратных
  скобках — как технические пометки на плёнке.
- Поверх всей страницы лежит зерно, оно рисуется скриптом
  в `script.js`.

## Приёмы, которые важно сохранить

- **Ровный срез.** Чёрный блок и то, что под ним,
  разделяет прямая линия во всю ширину. Рваных краёв и
  клякс больше не делаем — пробовали, не подошло.
- **Фотография сливается с фоном.** У картинки
  `mix-blend-mode: lighten`, поэтому её чёрный фон
  исчезает и статуя стоит прямо в черноте страницы, без
  рамки. Масок и растворений не добавлять.
- **Статую обрезает низ блока.** У `.figure` отрицательный
  нижний отступ, у `.dark` — `overflow: hidden`. Отсюда
  идеально ровный край.

## Что на странице заглушки

Подчёркнуты пунктиром (класс `.ph`):
имя в заголовке, город, род занятий. Ссылки в шапке пока
ведут в никуда. Владелец заменит это своим текстом.

## Что делать дальше

Доводим первый экран. Ниже пока ничего нет — предыдущие
секции удалены намеренно, не восстанавливать.

## Чего не делать

- Не добавлять библиотеки и сборщики. Только свой html/css/js.
- Не делать дизайн «вайбовым» и светлым — нужна мрачность.
- Не возвращать фото и звук обратно в base64 внутри html —
  решили держать их отдельными файлами рядом, так чище.
